
:root {
  --bg: #0e0f10;
  --panel: #191b1e;
  --panel-2: #22252a;
  --text: #f7f7f4;
  --muted: #a9adb5;
  --border: #343840;
  --accent: #f1c84b;
  --accent-text: #171717;
  --danger: #9b3d43;
}
* { box-sizing: border-box; }
body {
  margin: 0;
  background: var(--bg);
  color: var(--text);
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}
button, input, select { font: inherit; }
button { cursor: pointer; }
.app-shell { width: min(100%, 760px); margin: 0 auto; padding: 18px 14px 40px; }
.topbar, .section-heading, .row-between { display: flex; align-items: center; justify-content: space-between; gap: 12px; }
.topbar { padding: 8px 2px 14px; }
h1, h2, h3, p { margin: 0; }
h1 { font-size: 1.45rem; }
h2 { font-size: 1.1rem; }
h3 { font-size: 1rem; }
.eyebrow { color: var(--muted); font-size: .72rem; letter-spacing: .12em; margin-bottom: 4px; }
.panel { background: var(--panel); border: 1px solid var(--border); border-radius: 18px; padding: 16px; margin-bottom: 14px; }
.badge { background: var(--panel-2); border: 1px solid var(--border); border-radius: 999px; padding: 7px 10px; font-size: .82rem; }
.add-player { display: grid; grid-template-columns: 1fr auto; gap: 10px; align-items: end; }
label span { display: block; margin-bottom: 6px; color: var(--muted); font-size: .85rem; }
input, select { width: 100%; background: var(--panel-2); color: var(--text); border: 1px solid var(--border); border-radius: 12px; padding: 12px; }
button { border-radius: 12px; border: 1px solid transparent; padding: 12px 15px; font-weight: 700; }
.primary { background: var(--accent); color: var(--accent-text); }
.secondary { background: var(--panel-2); color: var(--text); border-color: var(--border); }
.danger { background: transparent; color: #ffb4b8; border-color: var(--danger); }
.small { padding: 8px 10px; font-size: .85rem; }
.message, .help { color: var(--muted); font-size: .86rem; margin-top: 9px; }
.empty { text-align: center; padding: 30px 10px; color: var(--muted); }
.empty h2 { color: var(--text); margin: 10px 0 4px; }
.logo-mark { width: 50px; height: 50px; border-radius: 50%; display: grid; place-items: center; margin: auto; background: var(--accent); color: var(--accent-text); font-size: 1.5rem; font-weight: 900; }
.hidden { display: none !important; }
.scoreboard { display: grid; gap: 9px; }
.player-row { display: grid; grid-template-columns: 36px 1fr auto auto; gap: 10px; align-items: center; padding: 11px; background: var(--panel-2); border: 1px solid var(--border); border-radius: 14px; }
.rank { width: 36px; height: 36px; display: grid; place-items: center; border-radius: 50%; background: var(--panel); }
.player-name { font-weight: 800; }
.player-status { color: var(--muted); font-size: .8rem; margin-top: 2px; }
.total { text-align: right; font-size: 1.5rem; font-weight: 900; }
.remove { background: transparent; color: var(--muted); border-color: var(--border); padding: 7px 9px; }
.stats { display: grid; grid-template-columns: repeat(4, 1fr); gap: 8px; margin-top: 16px; }
.stats div { padding: 10px; background: var(--panel-2); border-radius: 12px; text-align: center; }
.stats span { display: block; color: var(--muted); font-size: .72rem; }
.stats strong { display: block; margin-top: 4px; font-size: 1.1rem; }
.callout { margin-top: 12px; padding: 11px; border-radius: 12px; background: rgba(241,200,75,.13); border: 1px solid rgba(241,200,75,.35); }
.subsection { margin-top: 20px; }
.subsection p { color: var(--muted); font-size: .82rem; margin-top: 3px; }
.number-grid { display: grid; grid-template-columns: repeat(5, 1fr); gap: 8px; margin-top: 11px; }
.modifier-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; margin-top: 11px; }
.card-btn { min-height: 50px; background: var(--panel-2); color: var(--text); border-color: var(--border); }
.card-btn.selected { background: var(--accent); color: var(--accent-text); border-color: var(--accent); }
.toggles { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-top: 18px; }
.toggles label { background: var(--panel-2); border: 1px solid var(--border); border-radius: 12px; padding: 12px; }
.toggles input { width: auto; margin-right: 8px; }
.manual { display: block; margin-top: 12px; }
.actions { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-top: 16px; }
.actions.three { grid-template-columns: 1fr 1fr 1fr; margin-top: 0; }
.photo-preview { width: 100%; max-height: 330px; object-fit: contain; margin-top: 12px; border-radius: 12px; background: var(--panel-2); }
.round-card { background: var(--panel-2); border: 1px solid var(--border); border-radius: 12px; padding: 12px; margin-top: 10px; }
.round-scores { display: grid; grid-template-columns: repeat(2, 1fr); gap: 7px; margin-top: 8px; color: var(--muted); font-size: .85rem; }
dialog { border: 1px solid var(--border); border-radius: 18px; background: var(--panel); color: var(--text); width: min(92vw, 420px); }
dialog::backdrop { background: rgba(0,0,0,.75); }
.dialog-body { text-align: center; padding: 20px; }
.dialog-body button { width: 100%; margin-top: 18px; }
.trophy { font-size: 2.5rem; margin-bottom: 8px; }
@media (max-width: 520px) {
  .stats { grid-template-columns: repeat(2, 1fr); }
  .actions.three { grid-template-columns: 1fr; }
  .section-heading { align-items: flex-start; }
}
